#include<bits/stdc++.h>

using namespace std;

int main()
{
    cout << "Hello world!" << endl;
    vector<int> first_vector;
    first_vector.push_back(20);
    first_vector.push_back(10);
    cout<<first_vector[0]<<endl;;
    sort(first_vector.begin(),first_vector.end());
    cout<<first_vector[0];
    return 0;
}
